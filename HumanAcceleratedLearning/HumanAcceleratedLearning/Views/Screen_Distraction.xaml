<UserControl x:Class="HumanAcceleratedLearning.Views.Screen_Distraction"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:HumanAcceleratedLearning.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <StackPanel Orientation="Vertical" VerticalAlignment="Center" HorizontalAlignment="Center">

            <TextBlock HorizontalAlignment="Center"
                           FontSize="50"
                           Text="Your session will begin in"
                           TextWrapping="Wrap"
                           Visibility="{Binding Path=DistractorTextVisibility}"
                           />
            <TextBlock HorizontalAlignment="Center"
                           FontSize="96"
                           FontWeight="Bold"
                           Text="{Binding Path=DistractorTimeRemaining}"
                           Visibility="{Binding Path=DistractorTextVisibility}"
                           />

        </StackPanel>
    </Grid>
</UserControl>
